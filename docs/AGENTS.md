# Specialized agents

Audio, video/frame, effects, transitions, text/overlay, vision, color, motion/tracking, source matching, master editor, render, QC and autocorrection are separate stages. Each stage should expose a small JSON contract so models/tools can be replaced independently.