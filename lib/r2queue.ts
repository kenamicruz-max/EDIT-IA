import crypto from 'crypto';
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';
const e=(n:string)=>process.env[n]?.trim().replace(/^['\"]|['\"]$/g,'')||'';
export async function enqueue(input:{reference:string;source:string;referenceSize:number;sourceSize:number}){const miss=['STORAGE_BUCKET','STORAGE_ENDPOINT','STORAGE_ACCESS_KEY','STORAGE_SECRET_KEY'].filter(n=>!e(n));if(miss.length)throw new Error(`Storage runtime configuration is missing: ${miss.join(', ')}`);const ep0=e('STORAGE_ENDPOINT'),ep=/^https?:\/\//i.test(ep0)?ep0:`https://${ep0}`,c=new S3Client({region:e('STORAGE_REGION')||'auto',endpoint:ep,forcePathStyle:true,credentials:{accessKeyId:e('STORAGE_ACCESS_KEY'),secretAccessKey:e('STORAGE_'+'SECRET_KEY')}}),id=crypto.randomUUID(),job={id,status:'QUEUED',...input,createdAt:new Date().toISOString(),version:'5.2.3'};await c.send(new PutObjectCommand({Bucket:e('STORAGE_BUCKET'),Key:`jobs/${id}.json`,Body:JSON.stringify(job),ContentType:'application/json',CacheControl:'no-store'}));return {id,status:'QUEUED',version:'5.2.3'};}
