import { NextResponse } from 'next/server';
import { S3Client, PutObjectCommand, GetObjectCommand, DeleteObjectCommand } from '@aws-sdk/client-s3';
export const dynamic='force-dynamic'; export const runtime='nodejs';
const REQUIRED=['STORAGE_ENDPOINT','STORAGE_ACCESS_KEY','STORAGE_SECRET_KEY'] as const;
const clean=(value:string|undefined)=>value?.trim().replace(/^['\"]|['\"]$/g,'')||'';
const bucket=()=>clean(process.env.STORAGE_BUCKET)||'editar-ia-media';
export async function GET(){const configured=Object.fromEntries(REQUIRED.map(name=>[name,Boolean(clean(process.env[name]))]));configured.STORAGE_BUCKET=Boolean(bucket());const allConfigured=Object.values(configured).every(Boolean);let storage='not-tested';let storageError:string|null=null;if(allConfigured){try{const raw=clean(process.env.STORAGE_ENDPOINT),endpoint=/^https?:\/\//i.test(raw)?raw:`https://${raw}`;const client=new S3Client({region:clean(process.env.STORAGE_REGION)||'auto',endpoint,forcePathStyle:true,credentials:{accessKeyId:clean(process.env.STORAGE_ACCESS_KEY),secretAccessKey:clean(process.env.STORAGE_SECRET_KEY)}});const key=`health/${Date.now()}-${Math.random().toString(36).slice(2)}.txt`;await client.send(new PutObjectCommand({Bucket:bucket(),Key:key,Body:'EDIT-IA health probe',ContentType:'text/plain'}));const r=await client.send(new GetObjectCommand({Bucket:bucket(),Key:key}));await r.Body?.transformToString();await client.send(new DeleteObjectCommand({Bucket:bucket(),Key:key}));storage='ok';}catch(e){storage='error';storageError=e instanceof Error?e.message:'R2 connection failed';}}return NextResponse.json({ok:allConfigured&&storage==='ok',configured,storage,storageError,version:'5.2.7'},{headers:{'Cache-Control':'no-store, max-age=0'}});}
