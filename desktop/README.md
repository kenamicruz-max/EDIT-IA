# Desktop worker client

A desktop client can submit local media directly to a local worker for large files, avoiding browser/server upload limits. The same edit-spec contract is used by the web and worker layers.